runtime: python

env:
  SMITHERY_KEY: ${SMITHERY_KEY}
  GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
  GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
  SENDER_EMAIL: ${SENDER_EMAIL}
  SENDER_PASSWORD: ${SENDER_PASSWORD}
  SMITHERY_SCANNING: "true"